# ── Основная Telegram‑библиотека (user‑бот) ──
pyrogram[fast]==2.0.106     # [fast] подтянет tgcrypto для ускорения MTProto

# ── Ускоренный крипто‑backend (если [fast] не используется) ──
tgcrypto==1.2.5             # нужен Pyrogram для шифрования; ставится автоматом с [fast]

# ── Загрузка переменных из .env ──
python-dotenv==1.0.0

# ── Удобные логи ──
loguru==0.7.2